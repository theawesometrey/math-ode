package math.ode.vector;

/**
 * Enum that denotes a vector as being immutable or mutable.
 */
public enum VectorType {

    /**
     * Enum denoting a Vector that is immutable.
     */
    IMMUTABLE,

    /**
     * Enum denoting a Vector that is mutable.
     */
    MUTABLE
}
